query Runtime ($page: String!, $production: Boolean!, $locale: String!, $renderVersion: String!) {
  page(page: $page, production: $production, locale: $locale, renderVersion: $renderVersion) {
    messagesJSON
    pagesJSON
    componentsJSON
    extensionsJSON
    appsEtag
    appsSettingsJSON
  }
}
