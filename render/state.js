const {
  account,
  workspace,
  hash,
  route,
  version,
  culture: {
    locale,
  },
} = global.__RUNTIME__

const messages = {}

export default {
  account,
  workspace,
  hash,
  route,
  version,
  locale,
  messages,
}
